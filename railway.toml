[build]
builder = "NIXPACKS"

# Cache 2h em SQLite. Se existir volume no Railway (RAILWAY_VOLUME_MOUNT_PATH),
# o arquivo vai para esse disco; senão usa a pasta do app.
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
